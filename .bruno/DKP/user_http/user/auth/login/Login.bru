meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL_USER_HTTP}}/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user22@example.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
